import { styled  } from '@mui/system';

export const SignoutButton = styled('div')({
    display: 'flex', 
    justifyContent: 'center', 
    marginBottom: '16px'
  });

export const CreateItemButton = styled('div')({
    display: 'flex', 
    justifyContent: 'flex-end'
  });